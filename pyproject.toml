[project]
name = "innowise-tasks"
version = "0.1.0"
description = "Workspace for Innowise internship tasks"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0.1",
    "psycopg2-binary>=2.9.10",
]
